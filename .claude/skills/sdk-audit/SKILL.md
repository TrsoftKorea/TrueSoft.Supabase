---
name: sdk-audit
description: TrueBase SDK 정기 점검. 규칙 모순·규칙 위반·미사용 코드·중복 코드·비효율·문서 정합성·공개 표면을 축 단위로 검사하고 고친다. "SDK 점검", "정리하자", "audit" 같은 요청에 사용한다.
---

# SDK 정기 점검

SDK 전체를 **축 단위로 하나씩** 점검한다. 축 목록과 자동화 범위는 `Tools~/SdkAudit/README.md`의 "정기 점검 체크리스트"에 있다 — 이 스킬은 진행 방식만 정한다.

## 시작할 때

1. **검사기부터 돌린다.** 현재 무엇이 깨져 있는지가 출발점이다.

```bash
dotnet run --project Tools~/SdkAudit
```

2. **지난 회차 진행 상태를 확인한다.** 메모리의 `project_sdk_audit_progress.md`에 끝난 축·남은 축·사용자 결정 대기 항목이 있다. 없으면 새 회차다.

3. **어느 축을 돌지 사용자와 합의한다.** 임의로 여러 축을 한꺼번에 건드리지 않는다.

## 축 하나만 돈다

여러 축을 섞으면 무엇이 무엇을 깨뜨렸는지 가릴 수 없다. 특히 **미사용·중복·비효율 축은 동작을 바꿀 수 있어** Unity 재컴파일과 Play Mode 확인이 붙는다. 앞 축들이 정리된 뒤에 본다.

한 축 안에서는 이 순서로 한다.

1. **전수 조사 후 발견 목록부터 보고한다.** 고치기 전에 무엇이 나왔는지 먼저 알린다.
2. **오탐을 걸러낸다.** 검사기가 낸 경고라도 규칙의 정당한 예외일 수 있다.
3. **명확한 것만 고친다.** 판단이 갈리는 항목은 선택지와 근거를 제시하고 사용자가 정하게 한다.
4. **검증하고 보고한다.**

## 함정 — 이 리포지토리에서 실제로 겪은 것

**`CLAUDE.md`를 믿고 코드를 고치지 않는다.** 문서가 코드보다 낡아 있을 수 있다. "SupabaseSDK는 MonoBehaviour singleton"이라는 서술을 믿고 샘플이 `internal` 클래스를 부르게 바꿔 컴파일이 깨진 적이 있다. **항상 코드로 먼저 확인한다.**

**`Samples~`는 import 전까지 컴파일되지 않는다.** Unity 컴파일 통과가 샘플 검증이 아니다. 샘플이 부르는 API의 존재 여부는 검사기 R6이 본다.

**`Runtime/Unity`는 컴파일 검증 수단이 없다.** `CoreCompileCheck`는 Core만 본다. 검사기는 구문 파싱이라 타입 해석·using 누락을 못 잡는다. 이 계층을 고쳤으면 **사용자에게 Unity 재컴파일을 요청한다.**

**파일을 지우면 `.meta`도 함께 지운다.** 새로 만들지는 않는다.

## 검사기 오탐이 나오면

**규칙의 예외를 검사기에 반영한다.** 오탐을 그냥 두면 경고가 신호 구실을 못 한다. 예외 목록은 `Tools~/SdkAudit/README.md`의 예외 표에 근거와 함께 남긴다.

반대로 검사기가 **놓친 것**을 발견하면 규칙을 추가한다. 이번 회차에 샘플이 `SupabaseSDK.`를 부르는 걸 R6이 못 보던 공백을 그렇게 메웠다.

## 끝낼 때

```bash
dotnet run --project Tools~/SdkAudit
dotnet run --project Tools~/FailReasonCheck
dotnet build Tools~/CoreCompileCheck/CoreCompileCheck.csproj
```

문서를 건드렸으면 죽은 링크까지 확인한다.

```bash
cd docs~ && npx vitepress build
```

그리고 **메모리의 `project_sdk_audit_progress.md`를 갱신한다** — 끝난 축, 남은 축, 사용자 결정 대기 항목, 이번에 배운 함정. 컨텍스트가 압축돼도 여기서 이어갈 수 있어야 한다.

마지막으로 사용자에게 **축 하나가 끝날 때마다 커밋**을 권한다. 커밋 안 된 변경은 컨텍스트가 날아갔을 때 복구 수단이 없는 유일한 항목이다. 커밋·푸시는 사용자가 직접 한다.

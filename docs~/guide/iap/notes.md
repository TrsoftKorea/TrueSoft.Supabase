# 사용 시 알아둘 점

- **초기화 시점**: `CreateIAPAsync`는 로그인 완료 후에 호출합니다. 자동 로그인 경로에서도 동일합니다.
- **지원 범위**: 소모품(Consumable)을 지원합니다. 비소모품·구독은 아직 지원하지 않습니다.
- **정리**: `OnDestroy`에서 `Dispose()`를 호출해 이벤트 핸들러를 정리합니다.

---
